  const settingsBtn = document.createElement("button");
  const settingsContainer = document.createElement("div");
  const title = document.createElement("h1");
  const settings = document.createElement("div");
  const toggleThemeBtn = document.createElement("button");
  const status = document.createElement("div");
  const text = document.createElement("span");
  const disc = document.createElement("p");
  settingsContainer.className = "settings-container";
  settingsBtn.id = "NLE-Settings";
  settingsBtn.className = "NLE-Settings nlc-btn";
  settingsBtn.innerText = "Settings";
  title.className = "title";
  title.innerText = "NLE Settings";
  settings.className = "settings";
  toggleThemeBtn.id = "toggle-theme";
  toggleThemeBtn.className = "setting";
  status.className = "status active";
  status.innerText = "Active";
  text.className = "text";
  text.innerText = "Disable Theme";
  disc.className = "disc";
  disc.innerText = "Disables applied css";
  document.body.appendChild(settingsContainer);
  settings.appendChild(toggleThemeBtn);
  toggleThemeBtn.appendChild(status);
  toggleThemeBtn.appendChild(text);
  toggleThemeBtn.appendChild(disc);
  settingsContainer.appendChild(title);
  settingsContainer.appendChild(settings);
  nlc.appendChild(settingsBtn);